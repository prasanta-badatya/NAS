# Core framework
Django==5.2.11

# Production serving
waitress==3.0.2       # Windows WSGI server (local PC deployment)
gunicorn==23.0.0      # Linux WSGI server (Render / cloud deployment)
whitenoise==6.9.0
python-dotenv==1.1.0
djangorestframework==3.16.1

# CORS headers (allows Angular dev server to call the API)
django-cors-headers==4.9.0

# Image processing — thumbnails and EXIF date extraction
Pillow==12.1.1

# Video thumbnail extraction (frame grab). ffmpeg is tried first via subprocess;
# opencv-python is the Python fallback if ffmpeg is not on PATH.
opencv-python==4.11.0.86

# Django internal dependencies (pinned for reproducibility)
asgiref==3.11.1
sqlparse==0.5.3
tzdata==2025.2
